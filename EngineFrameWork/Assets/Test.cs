using cfg.ai;
using FrameWork;
using System.IO;
using UnityEditor;
using UnityEngine;
using System.Collections;
using System.Text;

public class Test : MonoBehaviour
{
    //// Start is called before the first frame update
    //IEnumerator Start()
    //{
    //    GameMgr.Ins.Startup();
    //    while (!GameMgr.Ins.Inited)
    //    {
    //        yield return null;
    //    }
    //    string path = "db/ai_tbblackboard.json";
    //    string assetPath = App.Env.AssetPath.Substring(App.Env.AssetPath.IndexOf("Assets")) + App.Env.ResourcesNoBuildPath + Path.AltDirectorySeparatorChar + path;
    //    var tar = AssetDatabase.LoadAssetAtPath(assetPath, typeof(TextAsset));

    //    //var tar = File.ReadAllText(assetPath, System.Text.Encoding.UTF8);
    //    //var result = App.Json.Decode<TbBlackboard>(cntent);
    //    //TbBlackboard result = new TbBlackboard(cntent);
    //    //Debug.Log(cntent);
    //}
}
